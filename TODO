More Precise Layout:
Move the pages so they touch at the binding edge on the page.
Center them on the page so that we have the most margin possible
to trim.


Spaces in filenames
Support for signature sizing based on user preferences.